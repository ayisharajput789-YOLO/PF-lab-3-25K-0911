#include <stdio.h>

int main(){
    int r = 7;
    printf("The area of circle with radius %d is %f\n ",r, 3.142*r*r);
      int height = 10;
    printf("The volume of cylinder with radius %d and height of %d is %f ",r,height,3.142*r*r*height);
    return 0;
}